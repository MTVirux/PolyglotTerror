# The project targets lf (see .editorconfig); normalise on the way in so an editor
# or script writing crlf cannot turn a small edit into a whole-file diff.
* text=auto eol=lf
*.png binary
*.tex binary
